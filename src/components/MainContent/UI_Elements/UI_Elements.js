import React from 'react';
import css from './UI_Element.module.css';

const UI_Elements = () => {
    return (
        <div className={css.elements_page}>
            <h1>UI Elements</h1>
        </div>
    )
};

export default UI_Elements;
